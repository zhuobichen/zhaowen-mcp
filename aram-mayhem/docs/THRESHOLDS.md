# 门槛登记表

> 由 `npm run thresholds:doc` 生成（`tools/audit-thresholds.mjs`）。**不要手改**。

这些数字散在各模块里，全库没有一处写明推导。这里把每个都登记出来，并算清它**买到的是什么**。

## 一、样本门槛（决定「能不能看出这个差」）

**可分辨差**：两组各 n 局、比胜率时，95% 置信下能分辨的最小差（百分点），算法 `1.96·√(0.5/n)`。
小于这个差，结论跟噪声分不开。

| 门槛 | 值 | 位置 | 分的是什么桶 | 可分辨差 | 为什么是这个数 |
|---|---|---|---|---|---|
| `minGames` | 1 | `lib/social.ts:130,131` | 队友 / 对手（本号同队过的 12 个账号） | 139pp | 实测（thresholds:sweep）：门槛 5→40 幸存账号 12→2，极差 41pp→15.5pp，比值 1.9~2.1 卡在线上。默认 1 等于不过滤，但那个 41pp 是 12 个账号里的极差（同样是最大值统计量） |
| `minGames` | 3 | `lib/patches.ts:164` | 补丁（通常 5~10 个） | 80pp | （统计上无关）只用于「列进明细」，不下结论 |
| `minGames` | 3 | `lib/social.ts:152` | 同上，判定用 1、下结论用 3 | 80pp | 同一个分析里两个门槛：榜单用 1（全列）、结论用 3（只对够局的账号下结论）—— 这个分层是有意的，但两处默认值不同且没有共用常量 |
| `perPairGames` | 5 | `lib/matchups.ts:193` | 我×对面 的组合（英雄视角） | 62pp | 实测（thresholds:sweep）：**这个门槛是惰性的** —— 本号那两个够样本的英雄（17 / 16 把），对位数在任何门槛（5~200）下都是 0。原因是它和外层的「英雄至少 15 把」互相打架：十几把摊到一百多个可能的对手上，没有谁碰得到 5 次。取值 5 是上一轮从 opts.minGames 拆出来时照抄的旧默认值，**没有任何依据**。处理方式：不改数（改了也没用，单元本来就薄），改成**在输出里说明为什么空** —— 「列不出对位：这个英雄只有 N 把，摊到一百多个可能的对手上，没有谁碰到 5 次以上」 |
| `minGamesPerWeek` | 5 | `lib/trend.ts:78` | 自然周 | 62pp | 未说明。5 局/周是全库最低的样本门槛，而它后面还要拿这个去下「有没有趋势」的结论 —— 实测（thresholds:sweep 相关那一节）：4 个有效周加起来也分辨不了 10 个百分点 |
| `minGames` | 8 | `lib/builds.ts:61` | 装备 × 槽位（成装约 150 件，本号够样本的 30~50 件） | 49pp | 实测（thresholds:sweep）：门槛从 5 升到 80，幸存条目 52→4，而「幸存者极差」从 69pp 一路缩到 5pp —— 典型的低门槛把噪声算进来了。但**极差是最大值统计量**（条目越多必然越大），不能当效应量读，所以这组数字只能说明「极差随门槛单调缩小」 |
| `minGames` | 10 | `lib/leaderboard.ts:22` | 账号（个位数） | 44pp | （统计上无关）上榜最低局数，不是统计门槛 |
| `minGames` | 10 | `lib/queue-stats.ts:123` | 队列（4~6 个） | 44pp | 实测（thresholds:sweep）：门槛从 5 扫到 200，**幸存队列始终是 1 个** —— 这个号的局几乎全在一个队列里，门槛定多少都不影响结论。在玩多个模式的账号上才会起作用 |
| `minGames` | 10 | `lib/queue-stats.ts:197` | 同上，注释文本里重读一次 | 44pp | 是同一个选项在文案里重读，值一致（10）。**但两处各写了一遍默认值**，改一处忘另一处就会让文案和判定对不上 |
| `minGames` | 12 | `lib/matchups.ts:179` | 对面出现过的英雄（约 170 个） | 40pp | 实测（thresholds:sweep）：门槛从 5 升到 20，条目 132→8、点名英雄的残差从 -32.3 → -20.3 → -17.9 一路缩小（典型的「低门槛把噪声算进来了」）；**30 就一个都不剩**。整段比值都在 1.4~2.0，即任何门槛下都跟噪声分不开 —— 所以结论里必须带「样本太少」的折扣（代码里已经有） |
| `minGames` | 12 | `lib/tft-detail.ts:137` | 棋子 / 装备（本号够样本的 90~110 个棋子） | 40pp | 实测（thresholds:sweep）：门槛 12→200，名次极差从 -4.3 缩到 -0.4，而比值一直在 0.5 上下 —— 整段都跟噪声分不开。也就是说棋子之间的名次差异这份数据看不出来，门槛高低都改变不了这一点 |
| `minGames`（共用常量） | 15 | `lib/combat-profile.ts:150` | 队内名次档（同上，6 个指标各分一次） | 36pp | **已与 get_my_contribution 合并成共用常量** `RANK_BUCKET_MIN_GAMES`（lib/thresholds.ts）。get_my_tilt 的 20 是**有意独立**的（它分的是连败/连胜长度桶，不是队内名次，桶的含义与分布都不同），已在常量文件里注明。实测（thresholds:sweep）：这几处在 5~80 之间都不改变结论 |
| `minGames`（共用常量） | 15 | `lib/contribution.ts:128` | 队内名次档（第 1 / 2 / 3 / 4 及以后） | 36pp | **已与 get_combat_profile 合并成共用常量** `RANK_BUCKET_MIN_GAMES`（lib/thresholds.ts）—— 两处是同一套「队内名次分档」，原先各写了一个 15，数值碰巧一样，但改一处忘另一处不会有任何提示。取值 15 本身仍然**未说明**：实测（thresholds:sweep）它在 5~80 之间都不改变结论，没有任何观测支持 15 比别的更好 |
| `minChampionGames` | 15 | `lib/matchups.ts:185` | 我玩过的英雄 | 36pp | 实测（thresholds:sweep，扫 3/5/8/10/12/15/20/30）：列出英雄数 34→22→15→12→7→**2**→**0**。所以 15 已经**贴着上限**（只剩 2 个英雄，20 就空了）—— 它的作用只是「别把只玩过两三次的英雄列出来」，不是统计门槛；数值本身没依据，但**上有硬约束**：再高列表就空了 |
| `verdictMinGames` | 15 | `lib/patches.ts:216,294` | 同上，但用于跨版本结论（第 216 行判定、第 294 行注释文本各读一次） | 36pp | 刻意比列明细的门槛高 —— 列出来是陈述事实，下结论才有样本要求 |
| `minGames` | 20 | `lib/empirical.ts:505` | 英雄 × 符文（二维交叉，单元最多） | 31pp | 实测（thresholds:sweep，按榜首样本量看）：门槛 5 时榜首是 5 局 100%（纯噪声）；20/25 时是 27 局 70.4%（约 2 倍标准误，临界）；30 时是 38 局 57.9%（0.6 倍，分不开）；**60 局以上一条都没有**。所以这个门槛确实偏低 —— 而且单元天生薄（玩得最多的英雄 264 局也凑不出 60 局的符文）。数字未说明，但**后果已在输出里处理**：get_champion_guide 现在逐条标「约是噪声的几倍」，首位不过 2 倍时表头降调成「可能特别搭，都还在噪声范围内」 |
| `minGames` | 20 | `lib/tilt.ts:101` | 连败/连胜长度档 | 31pp | 实测（thresholds:sweep）：门槛 20→40→80 会依次把「连输 3 把及以上」那档挤出去（3 档→2 档→1 档），效应也跟着从 -6.0 缩到 -2.9。默认 20 保住了三档，是有意义的下限。**这次扫描正是发现它结论过度声称的起因**（详见 lib/tilt.ts 注释） |
| `minGames` | 30 | `lib/empirical.ts:129` | 其他人的符文使用率 | 25pp | 实测（thresholds:sweep）：它排除的只是我自己那 306 局（占全库 3861 的 8%），所以候选数与单符文榜**逐行完全相同**（266/262/261/…/179）。门槛 30 保留 256/277（92%），基本不筛。与单符文榜的 100 是同一种统计、两个口径，数字不同但都没什么筛选作用 |
| `minGames` | 30 | `lib/empirical.ts:299` | 羁绊（约几十套） | 25pp | 实测（thresholds:sweep）：门槛 5~200 **全程 0 套够样本** —— 全库 3861 把里一次都没凑齐过。所以这个门槛本身不 bind，因为前提就不成立。工具的输出里已经说明了这是设计使然（羁绊要 4~8 件指定符文，而一局里多数人只有 4 个符文位），这条扫描只是**独立证实了它的那句话** |
| `minGames` | 30 | `lib/empirical.ts:683,712` | 同上，第 648/677 行各读一次 | 25pp | 同一个选项在判定与文案里各读一次，值一致（30） |
| `minGames` | 60 | `lib/comps.ts:149` | 对面 6 类标签 × 胜负 | 18pp | 实测（thresholds:sweep）：5~200 之间取值都不改变结论 —— 六类标签本来就都过线。效应（极差）只有 3.0pp，而噪声 4.6pp，比值 0.7：这个量级下根本看不出差别，门槛高低无所谓 |
| `minGames` | 60 | `lib/empirical.ts:228` | 符文两两组合（组合数远多于单件） | 18pp | 实测（thresholds:sweep）：组合有 **22164 个单元**，门槛 60 下仍留 455 条（2%）—— 这个是**真的在 bind**。数字本身仍未说明（方向还是反的：单元比单件多两个数量级、门槛反而更低），但**后果已经在输出里处理了**：现在会算出「从 455 条里挑最大、光噪声就有 ±19pp」并逐条标出各自的倍数 |
| `minGames` | 60 | `lib/empirical.ts:388` | 单件符文（与 empiricalAugments 同类） | 18pp | **未说明**：与 empiricalAugments 是同一类统计（单件符文的胜率），门槛却是 60 vs 100。实测（thresholds:sweep）那边 100 保住 210/277 条（基本不 bind），这边 60 更松 —— 两者的差别没有依据，只是两处各写了一遍 |
| `minGames` | 100 | `lib/empirical.ts:162` | 符文（约 200 件，本号归档 2 万行符文记录） | 14pp | 实测（thresholds:sweep）：277 件符文里，门槛 100 保留约 210 条（76%），噪声尺度 ±7.3pp —— **基本不 bind**（全归档 2 万行摊下来每件样本都够）。所以它更像「兜底，别把长尾露出来」，而不是在筛噪声。同族的另外两个口径见下面两条 |
| `minItemGames` | 150 | `lib/counters.ts:71` | 装备（成装约 150 件） | 11pp | 实测（thresholds:sweep）：门槛 5→120 幸存条目一直是 50，说明这些装备本来就都过线，门槛不 bind；极差 23pp 全程不变（同样是最大值统计量，别当效应量） |
| `minBucketGames` | 300 | `lib/counters.ts:72` | 对面阵容档 × 装备 | 8pp | 全库最高的门槛 —— 二维交叉，单元最多 |

## 二、差值阈值（决定「看出多大的差才值得说」）

**要分辨它需要多少局**：把阈值反解成所需样本（每组）。拿它跟该分析实际能拿到的样本比 ——
差得越远，说明这个阈值越像是「希望」而不是「能测出来的」。

| 判据 | 值 | 位置 | 讲的是什么 | 要分辨它需要 | 为什么是这个数 |
|---|---|---|---|---|---|
| `trend.lol` | 2 个标准误 | `lib/trend.ts` | 海斗周趋势：最近 4 个有效周 vs 之前 4 个，差多少才叫「有趋势」（现在按标准误倍数） | 不适用（不是比例） | 从固定 3pp 改成 2 倍标准误。实测：58 把 50.0% vs 42 把 40.5%，差 9.5pp 而噪声 10.0pp —— 原写法会报「是在变强」。口径：两个窗口各当成一个整体、按局数算标准误（周内不独立，已在结论里注明偏乐观） |
| `trend.tft` | 2 个标准误 | `lib/trend.ts` | 云顶周趋势：前段 vs 后段的平均名次差（现在按标准误倍数） | 不适用（不是比例） | 从固定 0.2 名改成 2 倍标准误。名次标准差约 2.2，10 局时标准误就有 0.7 —— 固定 0.2 分不开。实测：3.75 → 4.56，差 0.80 而噪声 0.16（5.0 倍），是真结论 |
| `patches.tft` | 2 个标准误 | `lib/patches.ts` | 云顶跨补丁：平均名次差多少才算「变好/变差」（现在按标准误倍数） | 不适用（不是比例） | 从固定 0.2 名改成 2 倍标准误：名次标准差约 2.2，15 局时均值差的标准误就有 0.57，固定的 0.2 分不开 |
| `patches.lol` | 2 个标准误 | `lib/patches.ts` | 海斗跨补丁：胜率差多少才算「变好/变差」（现在按标准误倍数） | 不适用（不是比例） | 从固定 3pp 改成 2 倍标准误，与云顶那边同口径。实测：27 局 vs 40 局差 4.4pp，噪声 12.3，0.36 倍 —— 原写法会报「新版好了 4.4」 |
| `contribution` | 1.5 个标准误 | `lib/contribution.ts` | 伤害队内第一 vs 第三及以后：差多少才算「有关系」（现在按标准误倍数，不再用固定百分点） | 不适用（不是比例） | 从固定 5pp 改成 2.5 倍标准误：固定阈值在 400 局的组里和 36 局的组里含义完全不同 |
| `tilt` | 2 个标准误 | `lib/tilt.ts` | 连败之后偏离基准多少才算「明显走低」（按标准误倍数） | 不适用（不是比例） | 从固定 5pp 改成 2 倍标准误：原来的写法在 36 局的桶上会拿 6.0pp 当「明显」，而它的标准误有 8.3 |
| `combat` | 1.5 个标准误 | `lib/combat-profile.ts` | 队内第一 vs 垫底：差多少才算「这项跟胜负有关」（现在按标准误倍数） | 不适用（不是比例） | 从固定 5pp 改成 2.5 倍标准误；同时给结论补上了各自的局数（只报百分比没法核对，也推不出噪声） |
| `combat.duration` | 2 分钟 | `lib/combat-profile.ts` | 两组的**中位时长**差几分钟以内算「时长差不多」（用来排除「久局」这个解释） | 不适用（不是比例） | 不是统计阈值，是「时长可比」的实用判断 |
| `compare.winrate` | 1.5 个标准误 | `lib/compare.ts` | 双账号：胜率差多少以内算「同一档」（现在按标准误倍数） | 不适用（不是比例） | 从固定 1.5pp 改成 1.5 倍标准误：两个账号局数常差很多，小号上固定 1.5pp 会得出「同一档」而噪声可能有好几个点。实测：差 1.9pp / 噪声 3.2（0.60 倍） |
| `compare.augment` | 0.02 | `lib/compare.ts` | 双账号（文字版）：符文偏好差多少才值得列 | 每组约 4,802 局 | 与下面 report-compare 那条是同一个语义，值却不同 |
| `reportcompare.augment` | 0.02 | `lib/report-compare.ts` | 双账号（HTML 版）：符文偏好差多少才值得列 | 每组约 4,802 局 | 与上面 compare.augment 是同一个语义 |
| `report.gap` | 10pp | `lib/report.ts` | 某英雄胜率比同批人高多少个百分点才列进「高光」 | 每组约 193 局 | 未说明 |
| `comps.noise` | 2 个标准误 | `lib/comps.ts` | 对面标签间的极差要超过多少个标准误才算「有影响」 | 不适用（不是比例） | 全库唯一一个**跟着样本量走**的差值判据，而且用的是 `2×(SE_下+SE_上)` 而不是 `√(SE²+SE²)` —— 更保守。实测：六类标签下噪声能造出的最大偏离约 √(2·ln 6) ≈ 1.9 个标准误，而这里要 4 个，**是全库唯一一处门槛高于噪声线的判据**（其余几处都是这次才发现门槛偏低） |
| `combat.degenerate` | 0.6 | `lib/combat-profile.ts` | 队内第一占比超过多少就认定「这项没有区分度」（退化检测，不是差值阈值） | 不适用（不是差值阈值） | 不是差值阈值，是「分组本身失效」的检测 |

## 三、这两类数要对得上账

样本门槛决定「能不能看出这个差」，差值阈值决定「看出多大的差才值得说」—— 两者是配套的。
如果差值阈值对应的样本需求**远大于**该分析实际拿得到的样本，那它报出来的「有趋势/有关系」
就可能是噪声。这不是说数字写错了，而是说：**它现在只能当方向参考，不能当结论。**

**大部分已经改成按标准误倍数**（9/14 条）：判据自己随样本量收紧，不再需要「这个数定多少」这种没法回答的问题。

**还剩下这些固定数字**，按「要分辨它需要的样本」从大到小排（同一语义的配对只列一次）：

| 判据 | 阈值 | 要分辨它需要 | 该分析的典型样本 | 措辞有没有打折扣 |
|---|---|---|---|---|
| `compare.augment` | 2.0pp | 每组约 4,802 局 | 分组样本通常几百 | **没有** |
| `report.gap` | 10pp | 每组约 193 局 | 几十~几百局 | 有 |

改造的样板是 `lib/comps.ts`（4 个标准误）—— 它是全库第一个跟着样本量走的判据，
其余几处（tilt / contribution / combat-profile / compare_accounts / patches / trend）都是照着它改的。
剩两个固定的：`compare.augment` 是「出现比例差」，两侧样本按比例算不出干净的标准误，
`report.gap` 是展示用的「高光」门槛（不主张因果，所以措辞本身就带折扣）。

## 四、内联在过滤条件里的阈值（第三类）

上面两张表原先只扫 `opts.X ?? N`，**看不见这一类** —— 而它们照样撑着一句主张：
例如 `buildsText` 的「出了它胜率最高（≥10 把）」就是在**所有 ≥10 把的装备里挑胜率最高**。
登记表当时还在说「全部登记」——扫描面不够，那句话就是假的。

| 位置 | 值 | 类别 | 撑着什么 / 为什么 |
|---|---|---|---|
| `lib/builds.ts:115` | `.games >=10` | 统计门槛 | 撑着「出了它胜率最高（≥10 把）」这句 —— 而它是在**所有 ≥10 把的装备里挑胜率最高**，是最大值统计量。实测（thresholds:sweep，按榜首样本量看）：门槛 5→15→30 时榜首分别是 6 局 83.3%（2.2 倍噪声）、18 局 77.8%（2.8 倍）、69 局 59.4%（1.6 倍）—— 10 把这一档榜首多半只有十来局，2 倍出头正是「从几十条里挑最大」能造出的水平 |
| `lib/builds.ts:129` | `.games >=15` | 统计门槛 | 撑着「出得多但胜率偏低（≥15 把，可以考虑换）」。与上一条同源、方向相反；实测同上：15 把时榜首 18 局，倍数 2.8 —— 同样落在「挑最大」能造出的范围里 |
| `lib/checkup.ts:193` | `.games >=15` | 统计门槛 | 从 ≥15 把的装备里挑胜率最低那条进体检。实测：候选 25 件，噪声线 22.8 个百分点，而挑出来的那条只有 0.3~0.7 倍噪声 —— **分不开**。已改成：分不开的这条 weight 记为 0（不参与「该看哪几条」的排序），并在文字里写明噪声线 |
| `lib/compare.ts:204,204` | `.games >=10` | 统计门槛 | 决定「两人都拿过的符文里胜率差最大的那几件」从哪些候选里挑。**也是最大值统计量** —— 已按同一套补上候选数 / 噪声尺度 / 逐条倍数（「胜率差最大的几件」= 从 N 件里挑差值最大） |
| `lib/compare.ts:84` | `.games >=5` | 统计门槛 | 管的是**符文偏好**那一栏（登记表原先写成「常玩英雄」，是我写错的 —— 代码里这个 `>= 5` 在 augmentPrefs 上）。实测（thresholds:sweep）：本号候选 75 个、噪声尺度 **±46.5 个百分点**。**这个门槛硬编码在 profileOf 里，探针改不了 —— 所以只量得到候选池形状、量不到拐点**，这一点如实记下 |
| `lib/comps.ts:170` | `.games >=30` | 统计门槛 | 实测（thresholds:sweep）：门槛 5~100 下，byCount 的 20 档**全部 ≥30 局** —— 这个内联值从不 bind（byCount 被截断到 20 档，样本本来就厚）。数值本身没依据，但对这份数据不起作用；真正的隐患是它和同文件的「opts.minGames ?? 30」**并存**，传参数就绕开它 |
| `lib/playstyle.ts:80` | `.games >=10` | 统计门槛 | 实测（thresholds:sweep）：四个时段的场次是 46/11/51/198 把，**全部 ≥10** —— 从不 bind。而且它是硬编码，探针改不了、只能从输出里读当前值。两处同值（时段与英雄池各一处），互不相关却用同一个数 |
| `lib/report-tft.ts:301` | `.games <15` | 显示阈值 | 同上 |
| `lib/report-tft.ts:373` | `.games <15` | 显示阈值 | 同上 |
| `lib/report-tft.ts:219` | `.games <5` | 显示阈值 | （统计上无关）图表里样本不足的柱子不标数字 |
| `lib/report.ts:1664` | `.games >=5` | 统计门槛 | 同上 |
| `lib/report.ts:1655,1662` | `.games >=8` | 统计门槛 | 撑着「建议」那一段（「用得比旁人好」）。同样是挑差值最大：实测候选 14 个，已补上候选数 / 噪声尺度 |
| `lib/report.ts:432` | `.games >=3` | 显示阈值 | （统计上无关）收集阶段的过滤，不直接对应某句话 |
| `lib/report.ts:1321` | `.games <15` | 显示阈值 | 同上 |
| `lib/report.ts:1623` | `.games >=5` | 统计门槛 | **同一个函数里 8 和 5 并存**，未说明为何不同。用在「英雄两极」（≥5 把里挑胜率最高最低）——实测候选 22 个、**噪声尺度约 39 个百分点**，而列出来的「顺手 80% / 该躲 29%」全在这个范围内。已在输出里写明「先当线索，别据此换英雄池」 |
| `lib/report.ts:1556,1569,1586` | `.games >=8` | 统计门槛 | 撑着报告里「发现」那一段的几条结论。8 把的标准误约 18 个百分点，而「真命符文」是**从这些里挑胜率最高**——实测候选 10 个、噪声尺度约 36 个百分点，榜首 8 把 88%（3.2 倍）刚好压线。已按同一套补上候选数 / 噪声尺度 / 逐条倍数 |
| `lib/report.ts:1373` | `.games <15` | 显示阈值 | 同上 |
| `lib/report.ts:1782` | `.games >=5` | 显示阈值 | （统计上无关）渲染阶段过滤 |
| `lib/report.ts:1269` | `.games <15` | 显示阈值 | （统计上无关）图表里样本不足的柱子不标数字 |
| `lib/report.ts:1451` | `.games <5` | 显示阈值 | 同上 |
| `lib/tools.ts:525` | `.games <30` | 统计门槛 | 「这个英雄在你归档里只打过 N 局，样本太少，不下结论」—— 这句判据本身，有意独立于符文层的门槛 |

共 21 处，其中 **12 处是统计门槛**（撑着一句主张）、9 处只影响图表显示。

共 26 个样本门槛 + 14 个差值阈值 + 21 处内联过滤登记在册；理由的状态：40 条有实测 · 14 条说明了理由但未量 · 6 条统计上无关（图表/列举用途） · 1 条一个字都没查过。
理由写不出来就写「未说明」—— 不要编一个听起来合理的，那比没有更坏，下一个人会照着它改。

## 怎么维护

新增样本门槛（`opts.min* ?? N`）后，`npm run audit:thresholds` 会报「没登记」；
在 `SAMPLE_REGISTRY` 加条目。新增差值阈值则在 `DIFF_REGISTRY` 加条目（带定位用的 `find` 正则）。
声明为同一语义的两条（`sameAs`）值不一致时会直接报错。
